mod vectors;
mod strings;

fn main() {
    vectors::vector_decls();
    vectors::vector_reads();
    vectors::vector_loop();

    strings::create();
    strings::update();
    strings::accessing_elements();
}
